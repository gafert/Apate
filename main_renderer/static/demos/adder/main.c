#include "sim.h"

/**
 * Hello, this is your starter file!
 *
 * Edit your code, compile it and simulate it.
 * If you have additional files add them in the config.json sources.
 * The gcc flags can also edited there.
 *
 * ATTENTION:
 * This CPU does not support multiplication, division, float or the standard library.
 * It does therefore not support malloc(), free(), etc.
 */

int main(void) {
  return 0;
}
